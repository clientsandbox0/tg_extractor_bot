# Services package
# Each service has a single, focused responsibility:
#
#   member_service      — fetch + persist members from old group
#   migration_service   — orchestrate the full migration flow
#   notification_service — send invite link DMs to members
#   tracking_service    — monitor joins on the new group
